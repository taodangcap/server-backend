{
  "name": "server-backend",
  "version": "1.0.0",
  "description": "Backend API using JSON Server",
  "main": "examples.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=14"
  }
}
