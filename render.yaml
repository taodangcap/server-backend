services:
  - type: web
    name: soundcloud-proxy
    env: node
    buildCommand: npm install
    startCommand: npm run dev
    envVars:
      - key: VITE_SOUNDCLOUD_CLIENT_ID
        sync: false
      - key: PROXY_PORT
        value: 3001



